const computerTurn: () => number = () => {
  console.log(
    "Randomly select 1 of the 4 computer moves, and return the move power, change the state to userTurn=true"
  );
  return 80;
};

export default computerTurn;

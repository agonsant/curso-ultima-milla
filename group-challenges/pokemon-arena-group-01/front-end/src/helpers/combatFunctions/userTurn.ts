const userTurn: () => number = () => {
  console.log(
    "The user select 1 of the 4 moves, and return the move power, change the state to userTurn=false"
  );
  return 100;
};

export default userTurn;

const calculateDamage: (movePower: number, actualHealth: number) => number = (
  movePower,
  actualHealth
) => {
  console.log(
    "Receives the move power, and the actualHealth of the oponent and return the new state health"
  );
  return 50;
};

export default calculateDamage;
